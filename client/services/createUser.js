import axios from 'axios'
const apiUrl = process.env.REACT_APP_API_URL;
const CreateUser = (formdata) =>
    {
        try
        {
            let res = axios.post(`${apiUrl}/api/login`,)
        }
    };